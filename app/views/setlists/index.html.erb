<h1>Setlists</h1>

<%= link_to 'New Setlist', new_setlist_path %>

<ul>
  <% @setlists.each do |setlist| %>
    <li>
      <%= link_to "[#{setlist.date}] #{setlist.title}", setlist_path(setlist) %>
    </li>
  <% end %>
</ul>
