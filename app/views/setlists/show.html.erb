<div class="header">
  <h1><%= @setlist.title %></h1>
  <h2><%= @setlist.date %></h2>
</div>

<div class="content">
  <div class="pure-g">
    <div class="pure-u-2-3">
      <% if !@setlist.notes.empty? %>
        <div>
          <h2>Notes</h2>
          <div style="white-space: pre;"><%= @setlist.notes %></div>
        </div>
      <% end %>
    </div>

    <div class="pure-u-1-3">
      <% if current_user.owns_setlist?(@setlist) %>
        <%= link_to 'Edit Setlist', edit_setlist_path(@setlist),
          :class => 'pure-button pure-button-primary'
        %>
        <%= link_to 'Edit Songs', setlist_edit_items_path(:setlist_id => @setlist.id),
          :class => 'pure-button pure-button-primary'
        %>
      <% end %>
    </div>

    <div class="pure-u-2-3">
      <h2>Songs</h2>

      <table class="pure-table pure-table-bordered">
        <thead>
          <tr>
            <th>
              Key
            </th>

            <th>
              Song
            </th>
          </tr>
        </thead>

        <tbody>
          <% @setlist_items.each do |song| %>
            <tr>
              <td>
                <%= song.key %>
              </td>

              <td>
                <%= link_to song.full_name,
                  song_path(song.song_id, :key => song.key),
                  :class => 'fancy-link'
                %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
